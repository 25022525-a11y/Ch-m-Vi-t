# Banh mi v10 Geometry Cleanup Implementation Plan

> Execute inline with executing-plans. This continues the approved local-refinement workflow; no scene rebuild, external assets, commits or installations.

**Goal:** Refine the existing v09 bread scores, crust and filling silhouettes before UV/material.

**Architecture:** Keep existing mesh connectivity and all object names/parents. Add V10 shape keys relative to V09 keys for reversible local deformations; reshape score neighborhoods rather than rebuilding bread. Repose existing filling pieces only. Preserve Camera/Light/root and preview setup.

**Tech Stack:** Blender 5.2.2 LTS through Blender MCP, bpy and bmesh validation.

## Constraints and outputs

- Source: D:/Chạm Việt/banh_mi_blockout_v09.blend; never overwrite it.
- Recovery: D:/Chạm Việt/banh_mi_recovery_backup_before_v10.blend; create only if absent.
- Output: D:/Chạm Việt/banh_mi_blockout_v10.blend.
- Previews: D:/Chạm Việt/v10_hero.png, v10_front.png, v10_side.png, v10_top.png.
- Baseline: 84 scene objects; 51,940 evaluated asset triangles. Aim for no more than 5% growth; no new subdivision.
- No tomato exists; preserve existing ingredient groups rather than introduce another ingredient.

## Tasks

- [x] Verify live v09, save copy before edits, record source hash, hierarchy, mesh counts and evaluated triangle baseline.
- [x] Reshape existing score neighborhoods into tapered, varied-width recesses and asymmetric crust lips; add low-amplitude crust and opening asymmetry. Validate closed topology and unchanged mesh connectivity.
- [x] Add meat edge/cupping/thickness variation and deliberate overlapping poses; round and cup existing leaf lobes; stagger cucumber/pickles/chili without adding objects.
- [x] Render hero first and inspect before all four angles. Preserve camera state and restore it after previews.
- [x] Check evaluated mesh manifold edges, nonzero face area, count growth, names/parents and protected transforms. Verify source SHA-256 unchanged; save only the new v10 file.
- [x] Report actual counts, observed improvements, residual reference mismatch and UV/material readiness without claiming photoreal accuracy.

Verified: 84 scene objects / 81 asset objects. 51,480 asset triangles after modifiers, down from 51,940 (-0.89%); 51,482 including preview ground. Base vertex/face counts and names/parents unchanged; root, Camera, Light and group transforms preserved. No evaluated non-manifold edges or faces below 1e-12 area. Source v09 SHA-256 unchanged. Backup and v10 saved, all four previews reviewed. Score boundary stair-stepping identified in first top render and corrected with continuous topology-aligned strips. Remaining: smooth blockout crust/crumb, simplified leaf and meat detail. Proceed to stylized UV/material preview, not a claim of photographic fidelity.
