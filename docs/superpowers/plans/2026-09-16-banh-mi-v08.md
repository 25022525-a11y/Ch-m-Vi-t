# Banh mi v08 geometry realism lock

**Goal:** Refine v07 into a coherent sliced baguette with dense, irregular three-layer filling. Preserve existing hierarchy and object names, Camera and Light. No final UV, texture or material pass.

**Approach:** Derive both bread halves from a common loft profile. Flatten the lower crust, gently rotate the upper cut plane around the rear hinge, and use score-aligned surface samples evaluated on the true crust profile. Keep crumb lips narrow and irregular. Repose existing ingredients into staggered clusters, not evenly spaced rows. Close the open cucumber-seed components.

**Tools:** Blender MCP, bpy/bmesh, existing basic materials. Work in the current scene and save only a new v08 file; no git operations or dependencies.

- [x] Inspect v07 transforms, parent relationships, modifiers and mesh defects; retain baseline.
- [x] Rework bread mesh data, then verify closed meshes and hero silhouette.
- [x] Recompose sixteen existing meat slices and eight cucumber pairs; reuse cilantro, chili and pickles with varied depth and tilt.
- [x] Close cucumber seed boundaries and inspect all asset meshes for non-manifold edges, zero-area faces and invalid numeric coordinates.
- [x] Render hero, front, side, top and inspect rear closure. Make bounded corrections if shape/spacing fails the brief.
- [x] Save banh_mi_blockout_v08.blend and v08_hero/front/side/top.png. Verify evaluated triangle count, names/parents and outputs. Report visual limitations honestly; geometry lock is not a photorealism claim.

## Verification result

84 scene objects; 81 asset objects including empties. Evaluated geometry: 51,408 triangles (v07: 43,368). No non-manifold edges or zero-area faces found in the evaluated asset meshes, including converted sauce curves. Existing names and parent relationships unchanged. Cucumber seed bases closed. Sauce curves remain editable with caps and a 0.000001-unit Weld modifier because Blender generated duplicate cap-boundary vertices. Bread retains 0.022-unit, two-segment bevels; meat retains subdivision level 1. Export should evaluate these modifiers.

Hero/front/side/top and additional back preview inspected. Rear closure and top/bottom massing are coherent; filling clustering and cucumber depth are less repetitive. Remaining: crust and crumb still smooth, leaf anatomy simplified, score interiors still visually graphic with flat materials. Recommend a bounded local realism cleanup before final UV/material work rather than another whole-scene rebuild.
